hi there

Work:
What i done:

Commands: 
- After creating a txt file. I run "git status" to check whether this branch is affected or not.

- Check for the URL. 'git remote -v'. After run "https://github.com/PrasanthDante03/twisam.git (fetch and push)"

- Add / Stage affected file. "git add ." / "git add text.text"

- Remove / UnStage affected file. "git reset text.txt" / "git reset directory"
- Remove / UnStage affected file. "git restore --staged text.txt" Experimental

- After adding the changes, give commit message. " git commit -m "Initial Commit" "

- Move Changes to Stage section.

- To check all branch "git branch"

- Create "git branch dev"

- Checkout "git checkout dev"

- Merge in "main" branch "git merge dev"